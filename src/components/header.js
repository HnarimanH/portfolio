function Header(){
  return (
      <div className="header">
        <a className="btn" href="#me">Me</a>
        <a className="btn" href="#works">My works</a>
        <a className="btn" href="#contact">Contact</a>
      </div>
  );
}

export default Header;